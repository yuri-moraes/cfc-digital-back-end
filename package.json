{
  "name": "cfc-digital-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "db:migrate": "node -e \"import('./src/db/pool.js').then(({initPool})=>initPool()).then(()=>import('./src/db/init.js')).then(({runMigrations})=>runMigrations()).then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)})\"",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --testEnvironment=node --runInBand",
    "test:watch": "node --experimental-vm-modules node_modules/.bin/jest --testEnvironment=node --runInBand --watch",
    "test:coverage": "node --experimental-vm-modules node_modules/.bin/jest --testEnvironment=node --runInBand --coverage",
    "lint": "echo 'Add eslint later'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@sentry/node": "^10.56.0",
    "@vercel/blob": "^2.4.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "pg": "^8.21.0",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "jest": "^30.4.2",
    "nodemon": "^3.1.14",
    "pino-pretty": "^13.1.3",
    "supertest": "^7.2.2"
  }
}
